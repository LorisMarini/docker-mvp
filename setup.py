import setuptools
from setuptools import setup

setup(name='dockermvp',
      version='0.1',
      description='App Description',
      url='url',
      author='First Last',
      author_email='',
      license='',
      packages=setuptools.find_packages(),
      zip_safe=False)
